/**
 * App-wide TouchableOpacity with immediate press-scale feedback + optional haptic.
 * Implementation lives in PressableFeedback (single source of truth).
 */
export {
  PressableFeedback,
  HapticTouchableOpacity,
  TouchableOpacity,
  type PressableFeedbackProps,
  type TouchableOpacityProps,
} from "@/components/PressableFeedback";
export { default } from "@/components/PressableFeedback";
