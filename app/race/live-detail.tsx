/**
 * Expo Router entry — Live Race detail.
 * Implementation lives in features/race (behavior-preserving extraction).
 */
import LiveRaceScreen from "@/features/race/screens/LiveRaceScreen";

export default LiveRaceScreen;
