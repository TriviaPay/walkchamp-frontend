/**
 * Expo Router entry — Walk tab.
 * Implementation lives in features/walk (behavior-preserving extraction).
 */
import WalkScreen from "@/features/walk/screens/WalkScreen";

export default WalkScreen;
